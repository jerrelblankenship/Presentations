var romanConverter = {
    convertToNumeric: function (a) {
        return '';
    },

    convertToRoman: function (num) {
        var roman = "";
        //while (num > 0) {
        //    roman = roman + "I";
        //    num--;
        //}
        return roman;
    }
}